{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "JwtSettings": {
    "Issuer": "OAuthSample",
    "SignKey": "asfdk112lasfodmmmeqq", /* length must more than 16 */
    "ExpiresMin": 480 
  },
  "AllowedHosts": "*",
  "Seq": {
    "ServerUrl": "http://localhost:5341/",
    "ApiKey": "",
    "MinimumLevel": "Information",
    "LevelOverride": {
      "Microsoft": "Warning"
    }
  },
  "DBConnectionString": {
    "DefaultConnectionString": "{{ConnectionString}}"
  },
  "OAuthSettings": [
    {
      "ProviderType": "LineLogin",
      "ClientId": "{{Client}}",
      "ClientSecret": "{{ClientSecret}}",
      "RedirectUri": "{{RedirectUri}}"
    },
    {
      "ProviderType": "LineNotify",
      "ClientId": "{{Client}}",
      "ClientSecret": "{{ClientSecret}}",
      "RedirectUri": "{{RedirectUri}}"
    }
  ]
}

